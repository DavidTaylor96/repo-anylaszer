{
  "name": "repo-analyzer",
  "version": "1.0.0",
  "description": "A tool that scans code repositories and generates documentation for Claude Projects",
  "main": "dist/main.js",
  "bin": {
    "repo-analyzer": "./dist/main.js"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/main.js",
    "dev": "ts-node src/main.ts",
    "clean": "rm -rf dist"
  },
  "keywords": ["repository", "analyzer", "documentation", "typescript"],
  "author": "Your Name",
  "license": "MIT",
  "devDependencies": {
    "typescript": "^5.0.0",
    "@types/node": "^20.0.0"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}